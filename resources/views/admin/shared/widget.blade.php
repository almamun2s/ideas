<div class="card">
    <div class="card-body">
        <h4><i class="{{ $icon ?? 'fas fa-user' }} "></i> {{ $title ?? 'Title' }}</h4>
        <h2 class="fw-bold">{{ $data ?? 0 }}</h2>
    </div>
</div>
